<h1>Add a new review to a <%= @restaurant.name.capitalize %> </h1>
<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit class: "btn btn-secondary" %>
<% end %>
